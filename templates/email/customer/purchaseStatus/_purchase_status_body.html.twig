 {{ include('email/customer/parts/_purchase_customer_infos.html.twig', {purchase: purchaseVendorGroup.purchase, lang}) }}

<h2 style="margin: 40px 0 20px; font-size: 1.5em; font-weight: 600;">Détail de la commande</h2>

<div style="margin: 20px 0 40px;">
    {{ include('email/customer/parts/_purchase_vendor_group.html.twig', {purchaseVendorGroup, lang}) }}
</div>

{{ include('email/customer/parts/_purchase_total.html.twig', {
    articlesPrice: purchaseVendorGroup.articlesPrice,
    shippingCost: purchaseVendorGroup.shippingInfo.price,
    totalPrice: purchaseVendorGroup.totalPrice,
    lang
}) }}


{{ include('email/customer/parts/_account_purchase_link.html.twig', {lang}) }}