
    <div style="margin: 40px 0;">
        <h2 style="font-size: 1.5em; font-weight: 600; margin-bottom: -10px;">Détail de la commande</h2>
        {% for index, purchaseVendorGroup in purchase.purchaseVendorGroups %}
            {{ include('email/customer/parts/_purchase_vendor_group.html.twig', {
                purchaseVendorGroup, 
                lang, 
                index,
                count: (purchase.purchaseVendorGroups | length)
            }) }}
        {% endfor %}
    </div>

    {{ include('email/customer/parts/_purchase_total.html.twig', {
        articlesPrice: purchase.articlesPrice,
        shippingCost: purchase.shippingCost,
        totalPrice: purchase.totalPrice,
        lang
    }) }}

